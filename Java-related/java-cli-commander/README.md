# Java CLI Commander

I made this practice project to explore executing CLI commands with Java, because I dislike writing bash scripts, which requires if statements or loops.

I do like the name Java CLI Commander... Idea: make a library, which makes it easy to write CLI scripts with Java.